add2 <- function(x ,y) {
  x+y
}
above <- function(x,n){
  use <- x > n
  x[use]
}
columnmean <- function(y) #will calculate mean of each coloumn and create a mean
{
  nc <- ncol(y)
  means <-numeric(nc)
for( i in 1:nc){
    means[i] <- mean(y[,i])
    }
    means
}
